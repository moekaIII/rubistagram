<h1 id="user-img"><%= image_tag @user.image.url %><%= @user.name %>さんのページ</h1>

<p>メールアドレス：<%= @user.email %><p>

  <h3>お気に入り一覧</h3>
<% @favorite.each do |favorite| %>
      <p><%= favorite.picture.title %></p>
<% end %>
